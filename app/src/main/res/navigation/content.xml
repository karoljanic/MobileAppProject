<?xml version="1.0" encoding="utf-8"?>
<navigation
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_nav_graph.xml"
    app:startDestination="@id/map_fragment">

    <!-- MAP -->
    <fragment
        android:id="@+id/map_fragment"
        android:name="org.mobileapp.MapFragment"
        android:label="Map"
        tools:layout="@layout/map_fragment">
    </fragment>

    <!-- SETTINGS -->
    <fragment
        android:id="@+id/settings_fragment"
        android:name="org.mobileapp.SettingsFragment"
        android:label="Settings"/>

    <!-- AR CORE -->
    <fragment
        android:id="@+id/ar_core_fragment"
        android:name="org.mobileapp.ArCoreFragment"
        android:label="ARCore Test"
        tools:layout="@layout/ar_core_fragment">
    </fragment>

</navigation>
